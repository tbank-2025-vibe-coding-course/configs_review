## Prompts
Начал с nginx.conf, за несколько промптов получил то что хотел, итоговый промпт использованный для двух других файлов. 

```
Мне нужно выполнить домашнее задание - найти ошибки и потенциальные уязвимости в заданных конфигурационных файлах. Исправь docker-compose.yml - внимательно изучи файл, найди все потенциальные ошибки и уязвимости, прямо в файле рядом с ошибкой / уязвимостью напиши комментарий поясняющий суть проблемы, опиcание решения и поменяй проблемную версию на исправленную. Я хочу, чтобы ты не создал новый файл, а исправил имеющийся, чтобы было удобно смотреть исправления в commit dif.

Также добавь в README.md краткий отчет (на каждое исправление - небольшой пункт, по сути в readme описание должно быть короче чем в комментариях самого файла) Начни с заголовка 
## docker-compose report
Далее не исользуй заголовки крупнее ### header
Можешь использовать жирный шрифт
```

Использовал Claude Code с Sonnet 4.5

## docker-compose report

### 🔴 Критические уязвимости безопасности:

1. **privileged: true в backend** - контейнер получал полный доступ к хосту с отключением всех мер безопасности Docker. Удален privileged режим.

2. **Монтирование /etc/shadow и системных файлов** - в контейнер монтировались критические файлы хоста включая хеши паролей пользователей (/etc/shadow, /etc/passwd, /etc/group) и секретный файл. Все опасные монтирования удалены.

3. **Хардкод слабых паролей БД** - пароли "toor" и "password" хранились в git. Заменены на переменные окружения MYSQL_ROOT_PASSWORD и MYSQL_PASSWORD.

4. **network_mode: host для Redis** - отключалась сетевая изоляция, порты открывались напрямую на хосте. Заменено на стандартные Docker networks.

### 🟠 Серьезные проблемы безопасности:

5. **Публичный порт MySQL 3306** - БД была доступна из внешней сети на 0.0.0.0:3306. Изменено на привязку к localhost (127.0.0.1:3306).

6. **MySQL 5.7 EOL** - использовалась версия с окончанием поддержки (октябрь 2023), без обновлений безопасности. Обновлено до MySQL 8.4 LTS.

7. **redis:latest без версии** - использование тега latest приводит к непредсказуемости и невоспроизводимости окружения. Зафиксирована версия redis:7.2-alpine.

## nginx report

### 🔴 Критические уязвимости безопасности:

1. **Логирование паролей в публичную директорию** - access.log с `$request_body` писался в `/var/www/production-api.host/public/`, доступный через веб
2. **HTTP Header Injection** - переменная `$userid` из URL вставлялась напрямую в заголовок X-User-Id без валидации
3. **Path Traversal через alias** - несоответствие слешей в `location /static` и `alias /prod_static/` позволяло читать файлы за пределами директории
4. **XSS через CSP** - использование `'unsafe-inline'` для script-src делает бесполезной защиту от XSS
5. **Синтаксическая ошибка в Lua** - неправильное чтение заголовков и опечатка в условии для `/management/`

### 🟡 Серьезные ошибки конфигурации:

6. **keepalive_timeout 65536** - таймаут ~18 часов вместо 65 секунд приведет к исчерпанию соединений
7. **internal в PHP location** - делает PHP-файлы недоступными для прямых запросов
8. **Неправильный вложенный location** - regex location внутри prefix location не работает как ожидается
9. **if внутри location с add_header** - заголовки безопасности теряются при срабатывании условия CORS
10. **Отсутствие определения $base** - в третьем server блоке не была определена переменная

## apache2.conf report

### 🔴 Критические уязвимости безопасности:

1. **User root / Group root** - Apache запускался с правами суперпользователя. При компрометации приложения злоумышленник получит полный контроль над сервером. Изменено на www-data.

2. **Options Includes в /usr/share** - Server Side Includes с возможностью выполнения команд через `<!--#exec cmd="...">`. Изменено на IncludesNOEXEC.

3. **Закомментирована защита .htaccess/.htpasswd** - конфиденциальные файлы с хешами паролей и настройками доступны через HTTP. Раскомментирован блок FilesMatch.

### 🟠 Серьезные проблемы безопасности:

4. **Options Indexes в /var/www/** - включен листинг директорий, раскрывает структуру приложения и файлы. Удалена опция Indexes.

5. **LogLevel emerg** - логируются только критические системные ошибки, пропускаются важные события безопасности и ошибки. Изменено на warn.

### 🟡 Проблемы производительности:

6. **Timeout 600** - таймаут 10 минут приводит к исчерпанию пула соединений при медленных клиентах. Уменьшен до 60 секунд.

